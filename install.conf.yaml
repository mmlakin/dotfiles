- defaults:
    link:
        create: true
        relink: true

- clean: ['~']

- create:
    - ~/src

- link:
    ~/.zshrc:
      force: true

- shell:
  - [git submodule update --init --recursive, Installing submodules]
